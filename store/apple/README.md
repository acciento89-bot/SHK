# Canonical iOS AppIcons

These five 1024×1024 PNGs are generated deterministically by
`scripts/generate_app_icon.py`, the same generator used for the iOS App Store
archives. Every Android flavor uses its corresponding PNG byte-for-byte as the
direct launcher icon. Adaptive and round-icon variants are intentionally absent.
